<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record id="document_hub_document_tree_view" model="ir.ui.view">
            <field name="name">Document hub document tree view</field>
            <field name="model">document_hub.document</field>
            <field name="arch" type="xml">
                <tree string="Documents">
                    <field name="name" />
                    <field name="folder_id" optional="show" />
                    <field name="description" optional="show" />
                    <field name="tag_ids" optional="show" />
                    <field name="partner_id" optional="hide" />
                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" />
                </tree>
            </field>
        </record>

        <record id="document_hub_document_form_view" model="ir.ui.view">
            <field name="name">Document hub document form view</field>
            <field name="model">document_hub.document</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <div class="oe_title">
                            <label for="name"/>
                            <h1>
                                <field name="name" placeholder="e.g. Project contract" />
                            </h1>
                        </div>
                        <label for="tag_ids"/>
                        <field name="tag_ids" widget="many2many_tags" />
                        <group name='general' string='General' col="2">
                            <group col='2'>
                                <field name="file_ids" widget="many2many_binary" string="Upload files" nolabel="1" colspan="2"/>
                            </group>
                            <group col='2'>
                                <field name="folder_id" />
                                <field name="partner_id" />
                                <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" />
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers" groups="base.group_user" />
                        <field name="activity_ids" widget="mail_activity" />
                        <field name="message_ids" widget="mail_thread" />
                    </div>
                </form>
            </field>
        </record>

        <!-- <record id="document_hub_document_kanban_view" model="ir.ui.view">
            <field name="name">Document hub document kanban view</field>
            <field name="model">document_hub.document</field>
            <field name="arch" type="xml">
                <kanban js_class="document_kanban" create="false" sample="1">
                    <field name="id" />
                    <field name="owner_id" />
                    <field name="file_type" />
                    <field name="create_uid" />
                    <field name="create_date" />
                    <field name="active" />
                    <field name="name" />
                    <templates>
                        <t t-name="kanban-box">
                            <div draggable="true" class="d-flex flex-column oe_kanban_global_area o_kanban_attachment o_documents_attachment" t-att-data-id="record.id.raw_value">
                                <t t-set="fileRequest" t-value="record.file_type.raw_value === 'empty'" />
                                <div class="o_kanban_image" t-attf-class="#{fileRequest ? 'o_request_image' : ''}">

                                </div>

                                <div class="o_kanban_details flex-grow-1 flex-shrink-1 flex-auto">
                                    <div class="o_kanban_details_wrapper">
                                        <div t-att-title="record.name.raw_value" class="o_kanban_record_title">
                                            <field name="name" required="True" string="Document" />
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body">
                                    <field name="tag_ids" class="d-block text-wrap" widget="many2many_tags" />
                                </div>

                                <div class="o_kanban_record_bottom flex-wrap">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="owner_id" widget="many2one_avatar_user" />
                                    </div>
                                </div>

                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record> -->

        <record id="document_hub_document_act" model="ir.actions.act_window">
            <field name="name">Documents</field>
            <field name="res_model">document_hub.document</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Let's get start. Upload new document!
                </p>
            </field>
        </record>


    </data>
    <menuitem name="Document hub" id="menu_root" web_icon="document_hub,static/description/icon.png" />
    <menuitem name="Documents" id="menu_document_hub_document" parent="menu_root" sequence="10" action="document_hub_document_act" />
</odoo>